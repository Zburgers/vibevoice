# Temporary PR trigger for the trusted default-branch inspector.
name: Inspect VibeVoice 0.2.6 release

on:
  pull_request:
    branches:
      - master
    paths:
      - .github/workflows/inspect-v0.2.6-release.yml

permissions:
  actions: read
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "The authoritative release inspection runs from master."
