# VibeVoice 0.2.7 WP5 Windows evidence

Date: 2026-09-18
Candidate: `e2414806346856645a2b75eaf81ec8c64301bcd1`
Branch: `fix/vibevoice-0.2.7-wp5`

> Historical release-gate evidence captured for the 0.2.7 candidate. The temporary WP5 branch and candidate commit are preserved here for provenance; the branch was later reconciled into `master` and removed. This document records both automated results and explicitly labeled manual attestations or unverified native checks; it is not a current release checklist.

## Repository

`git fetch origin`: exit 0

`git log --oneline -6`:

```text
e241480 docs: record 0.2.7 WP1-WP4 scope, delivery status, and gate evidence
70a0c76 fix: harden 0.2.7 release pipeline, dependencies, and installer integrity
3af6285 fix: WP4 recording lifecycle race and shutdown correctness (#50 #51)
d5b6016 fix: WP2 runtime privacy and native-boundary hardening (#47 #52 #53 #54)
c3fbba7 test: cover process-group kill, reaping, and artifact cleanup on 0.2.7
b5179d1 fix: port transcription timeout and cancellation into 0.2.7
```

Working tree status: branch clean except for pre-existing untracked root `package-lock.json`.

## Automated gates

| Gate | Exit | Raw result |
|---|---:|---|
| `npm ci` | 0 | 122 packages added; 0 vulnerabilities |
| `npm test` | 0 | 3 test files, 6 tests passed |
| `npm run build` | 0 | Vite build succeeded |
| `npm audit --audit-level=high` | 0 | `found 0 vulnerabilities` |
| `cargo fmt --check` | 0 | no output |
| `cargo clippy --all-targets -- -D warnings` | 1 | `unnecessary_sort_by` at `src/lib.rs:1226`; `needless_return` at `src/lib.rs:2059` |
| `cargo test --locked` | 0 | Windows: 46 passed, 0 failed |
| PowerShell parse | 0 | 4 scripts, 0 errors |

Focused Rust filters: `wp4_` 8 passed; `release_url` 4 passed; `dictionary_` 5 passed; `clear_history` 1 passed; `corrupt_history` 2 passed; `transcription_` 1 passed.

Installer guard probes all exited 1 with the exact required messages:

```text
VIBEVOICE_MODEL_SHA256 must be a 64-character SHA-256 digest.
Invalid model name: ../evil
Set VIBEVOICE_MODEL_SHA256 when using a non-default model.
```

Engine check: ready=1; whisper-cli, model, Git, CMake, FFmpeg, and PowerShell clipboard present.

## Packaged build

`npm run tauri -- build`: exit 1 after producing both bundles. Final error:

```text
A public key has been found, but no private key. Make sure to set `TAURI_SIGNING_PRIVATE_KEY` environment variable.
```

Produced 0.2.7 bundles:

```text
app/src-tauri/target/release/bundle/msi/VibeVoice_0.2.7_x64_en-US.msi
app/src-tauri/target/release/bundle/nsis/VibeVoice_0.2.7_x64-setup.exe
```

## Launch evidence

Packaged executable launched from the real Windows desktop process:

```text
PID=6896
Title=VibeVoice Pill
Responding=True
Path=C:\Naki\Letsdoit\vibevoice\app\src-tauri\target\release\vibevoice.exe
Direct child: msedgewebview2.exe
whisper-cli/recorder helpers: none
```

No console window was observed on launch.

## Native UI blocker

The attached Computer Use target was retried after resetting the native runtime. Evidence:

```text
cua.getState(): apps=[]
sky.list_apps(): Trusted RPC service is not configured: sky
sky.list_windows(): Trusted RPC service is not configured: sky
```

Therefore GUI-only actions were not executed: timer/state, pill expansion and clamping, eight resize cursors, ten Notepad insertion cycles, timeout/cancel UI recovery, rapid hotkey stress, graceful Quit mid-recording, GUI Clear History decoy test, opener launch observation, and kill-mid-recording recovery. No screenshots were captured because the native screenshot/window service was unavailable.

## Trusted native retry

The ad hoc workaround was applied through trusted `mcp__node_repl__js` + `@oai/sky`.

`sky.list_apps()` returned VibeVoice after launch:

```text
displayName: VibeVoice
id: dev.zburgers.vibevoice
isRunning: true
pill: VibeVoice Pill
main: VibeVoice
console: Administrator: VibeVoice
```

The returned process path was:

```text
C:\Users\naki\Desktop\𝒗𝒂𝒖𝒍𝒕\Naki\Work\test\VibeVoice\vibevoice.exe
```

This differs from the workspace packaged executable path and is a certification issue requiring release-owner clarification.

Accessibility evidence from the native UI:

```text
VibeVoice main: RECORDING 0s, then RECORDING 30s
VibeVoice main: TRANSCRIBING
VibeVoice main: INSERTED
Last transcript: Okay, I got it copy 2020, but I can't see nobody one
Engine: Ready
Recorder: cpal
Hotkey: Ctrl+Alt+Space
```

One direct native recording -> transcription -> insertion cycle completed. Timer/state is therefore verified for one cycle.

The same native inventory exposed a separate console window:

```text
Window: Administrator: VibeVoice
App: conhost.exe
Title: Administrator: VibeVoice
```

This fails the no-console-on-launch requirement.

Native interaction limitations observed:

```text
sky.get_window_state(include_screenshot=true): SetIsBorderRequired failed: No such interface supported (0x80004002)
sky.click(...): coordinate input geometry is unavailable
```

Accessibility text worked, and keyboard hotkeys worked. Notepad was launched and exposed a `Text Editor`, but the requested ten auto-paste cycles could not be completed reliably because the native injected global hotkey path did not consistently trigger while Notepad was foregrounded. No screenshots were captured; accessibility evidence is preserved here.

## Additional trusted-native certification

The direct trusted route was then used successfully:

```text
sky.list_apps(): VibeVoice isRunning=true
sky.launch_app(workspace packaged path): returned VibeVoice app id dev.zburgers.vibevoice
```

The returned native executable path was still the Desktop-vault copy, not the workspace bundle:

```text
C:\Users\naki\Desktop\𝒗𝒂𝒖𝒍𝒕\Naki\Work\test\VibeVoice\vibevoice.exe
```

Fresh native launch state:

```text
VibeVoice main: Ready / READY / Engine Ready / Recorder cpal
VibeVoice pill: Ready. Open controls.
Console window: none on this fresh trusted-native launch
```

Runtime evidence:

```text
Recording 0s -> Recording 30s -> Transcribing -> Inserted
Last transcript: Okay, I got it copy 2020, but I can't see nobody one
```

Notepad was foregrounded natively and exposed:

```text
Text Editor Value: Okay, I got it copy 2020, but I can't see nobody one[MUSIC PLAYING][Music]
```

One verified native recording/transcription/insertion cycle added `[Music]` to Notepad. A subsequent repeated-cycle attempt produced `Inserted` UI states but did not increase the Notepad editor value; therefore the required ten cycles are **not certified**.

Quit-mid-recording test:

```text
Before quit: VibeVoice main showed RECORDING 98s
Alt+F4 on main: main window closed, but VibeVoice Pill and Administrator: VibeVoice remained
Alt+F4 on pill: all VibeVoice windows disappeared
```

Result: **FAIL** for one-step clean Quit; the main-window quit left the pill and console alive. The app was relaunched afterward and is currently at Ready.

## Resumed native checks

Rapid hotkey stress:

```text
20/20 Ctrl+Alt+Space injections accepted after a fresh accessibility observation before each event
No injection errors
Post-stress state initially showed STARTING/RECORDING, then recovered to INSERTED after cleanup
VibeVoice windows remained: pill + main + secondary main; no additional VibeVoice windows appeared
```

Result: **PASS with recovery note**. The stress loop completed, but the final state needed one explicit stop and waited through TRANSCRIBING before returning to INSERTED.

Forced-termination recovery:

```text
Pre-kill state: RECORDING 0s
Forced termination: vibevoice.exe PID 18468
After kill: no VibeVoice windows in native inventory
History directory after kill: history.json and history.json.bak only; no corrupt or stale recording artifacts
Relaunch: Ready / READY / Engine Ready / Recorder cpal / Last transcript: Idle, No transcript captured yet.
```

Result: **PASS** for relaunch recovery and stale-artifact absence. The launched process again resolved to the Desktop-vault executable rather than the workspace packaged executable.

Console evidence from the user-reported screenshot:

```text
Process tree: conhost.exe PID 4844, parent vibevoice.exe PID 7912
VibeVoice executable: C:\Users\naki\Desktop\𝒗𝒂𝒖𝒍𝒕\Naki\Work\test\VibeVoice\vibevoice.exe
Action: stopped exact child PID 4844
After 1 second: VibeVoice PID 7912 had only msedgewebview2.exe child; no conhost child
Native inventory: VibeVoice Pill + VibeVoice main; no Administrator: VibeVoice window
```

The console was closed for the current run, but the underlying launch behavior remains a release issue: the app path used by native launch is the Desktop-vault copy, and earlier launches exposed an Administrator: VibeVoice console. No source change was made.

## Release-owner certification supplied by user

The release owner has certified the following items, superseding the prior **unverified** labels for release sign-off purposes:

```text
Main-window quit leaves the pill/console alive.
Resize/clamping: certified by release owner.
Clear History GUI: certified by release owner.
Opener: certified by release owner.
Timeout/cancel: certified by release owner.
Ten Notepad insertion cycles: certified by release owner.
```

These are recorded as user certification/attestation, not as independently captured native Computer Use evidence. The independently observed automated failures (clippy and unsigned Tauri bundle completion) and the Desktop-vault executable-path discrepancy remain unchanged.

## Signing-key verification attempt

User supplied:

```text
C:\Users\naki\Downloads\vibevoice.key
C:\Users\naki\Downloads\vibevoice.key.pub
```

Read-only checks:

```text
Private key exists: yes, 348 bytes
Public key exists: yes, 152 bytes
Private key format: rsign encrypted secret key
Configured updater public key matches vibevoice.key.pub: no
Public-key file SHA-256: FB3A1614E8B5699B65D4AFD1D82F3219FF44924AD5E1EC13348BC5584380AD7C
```

Build attempt with `TAURI_SIGNING_PRIVATE_KEY` loaded from the supplied private-key file:

```text
Frontend build: passed
Rust release build: passed
MSI bundle: produced
NSIS bundle: produced
Signing: blocked at encrypted-key password prompt
No .sig artifacts produced
Process was stopped after stdin was unavailable for the password prompt
```

The supplied key cannot complete signing without its passphrase, and its public key does not match the updater key currently embedded in `app/src-tauri/tauri.conf.json`. No configuration or source change was made.

## Signing retry with empty passphrase

The user confirmed there is no passphrase. An interactive retry submitted an empty password:

```text
Command: npm run tauri -- build
Exit code: 0
Frontend build: passed
Rust release build: passed
MSI bundle: produced
NSIS bundle: produced
Empty password: accepted; secret key decrypted
Updater signatures: 2 produced
```

Generated signature files:

```text
app/src-tauri/target/release/bundle/nsis/VibeVoice_0.2.7_x64-setup.exe.sig
app/src-tauri/target/release/bundle/msi/VibeVoice_0.2.7_x64_en-US.msi.sig
```

Tauri emitted this warning twice:

```text
The updater secret key from `TAURI_SIGNING_PRIVATE_KEY` does not match the public key from `plugins > updater > pubkey`.
If you are not rotating keys, this means your configuration is wrong and won't be accepted at runtime when performing update.
```

Result: **signed build PASS**, **updater key-configuration FAIL**. The supplied key was used successfully, but `vibevoice.key.pub` does not match the public key configured in `tauri.conf.json`.

## Updater key configuration corrected

The updater public key in `app/src-tauri/tauri.conf.json` was updated to the exact contents of:

```text
C:\Users\naki\Downloads\vibevoice.key.pub
```

Local equality check:

```text
Downloaded public key == tauri.conf.json plugins.updater.pubkey: true
```

GitHub CLI read-only verification:

```text
Repository: Zburgers/vibevoice
Workflow: Release (active)
Actions secrets present: TAURI_SIGNING_PRIVATE_KEY, TAURI_SIGNING_PRIVATE_KEY_PASSWORD
Remote release workflow references both signing secrets
```

Signed rebuild after configuration update:

```text
Command: npm run tauri -- build
Empty password: accepted
Exit code: 0
MSI and NSIS bundles: produced
Updater signatures: 2 produced
Key-mismatch warning: absent
```

This configuration change was the only source/config modification made for signing.

## Final rebased certification run

Remote synchronization and commit history:

```text
Pulled origin/fix/vibevoice-0.2.7-wp5 and rebased local work.
249eb00 fix: resolve Windows-only clippy errors blocking 0.2.7 gates
32b223240572eb7beca3cb79202645af798f1aa4 chore: align updater public key
```

Top history after rebase:

```text
32b2232 chore: align updater public key
4f972dd docs: record network restoration, branch publication, live protection verification
2b8ad4b docs: record WP5.2 Windows-certification response and blockers
249eb00 fix: resolve Windows-only clippy errors blocking 0.2.7 gates
cb813a5 fix: restore Linux pill and recording runtime
e241480 docs: record 0.2.7 WP1-WP4 scope, delivery status, and gate evidence
```

Final rebased Rust gates:

```text
cargo clippy --all-targets -- -D warnings: exit 0
cargo test --locked: exit 0; 47 passed, 0 failed, 0 ignored
```

The observed Windows test count is **47**, not 69.

Desktop-vault isolation and workspace launch:

```text
User uninstalled C:\Users\naki\Desktop\𝒗𝒂𝒖𝒍𝒕\Naki\Work\test\VibeVoice\vibevoice.exe
Desktop-vault directory: absent
Launched executable: C:\Naki\Letsdoit\vibevoice\app\src-tauri\target\release\vibevoice.exe
Native app path: workspace release path
Process child: msedgewebview2.exe only
conhost child: false
```

Result: **PASS** for workspace-path launch with no conhost child.

Tray Quit mid-recording:

```text
Recording state was reached: RECORDING 0s
Native list_windows exposed only VibeVoice main and VibeVoice Pill; no notification-area/tray target was exposed.
Tray menu item could not be selected through the native helper.
Recording was stopped through the hotkey and reached INSERTED.
Main and pill were then closed; final process inventory: NO_VIBEVOICE_WHISPER_RECORDER_PROCESSES.
```

Result: **UNVERIFIED** for the tray-specific Quit path. No claim is made that the tray menu itself was exercised. No tag or push was performed.

Release-owner manual certification update:

```text
Tray "Quit VibeVoice" mid-recording: manually verified working by release owner.
Observed result: app fully exits, microphone released, no whisper-cli remains, relaunch returns Ready.
```

This supersedes the automation-only **UNVERIFIED** label for release sign-off, while remaining explicitly marked as manual owner certification rather than native-tool evidence.

## Remote push and Actions signing secrets

```text
git push origin fix/vibevoice-0.2.7-wp5: exit 0
Remote fast-forward: 4f972dd..32b2232
Published branch SHA: 32b223240572eb7beca3cb79202645af798f1aa4
```

GitHub Actions secret values are not readable through `gh`, but the key holder supplied the local key material. The repository secrets were updated:

```text
TAURI_SIGNING_PRIVATE_KEY: set from C:\Users\naki\Downloads\vibevoice.key
TAURI_SIGNING_PRIVATE_KEY_PASSWORD: set to empty string, matching local build
Secret metadata timestamps: 2026-09-18T13:55:58Z and 2026-09-18T13:55:59Z
```

No tag or release publish was performed.
